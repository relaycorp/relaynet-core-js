import { describeMessage } from './_test_utils';
import { CargoCollectionAuthorization } from './CargoCollectionAuthorization';

describe('CargoCollectionAuthorization', () => {
  describeMessage(CargoCollectionAuthorization, 0x44, 0x0);
});
