import RelaynetError from '../RelaynetError';
/**
 * Error thrown when there was a failure in the communication with the backing service.
 */
export declare class KeyStoreError extends RelaynetError {
}
